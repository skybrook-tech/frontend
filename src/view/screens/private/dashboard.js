import React from "react";
import PropTypes from "prop-types";

const NotFound = props => {
  const {} = props;

  return (
    <div
      data-testid="dashboardPage"
      className="flex justify-center items-center fit-parent"
    >
      <p>Dashboard</p>
    </div>
  );
};

export default NotFound;
